import React from 'react';

const MovieDetailsPage = ({params}: any) => {
    // console.log(params)
    return (
        <div>
            movie
        </div>
    );
};

export default MovieDetailsPage;